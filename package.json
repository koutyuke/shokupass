{
  "name": "shokupass",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "fmt": "turbo fmt",
    "test": "turbo test",
    "all": "pnpm --filter @shokupass/*",
    "mobile": "pnpm --filter @shokupass/mobile",
    "web": "pnpm --filter @shokupass/web",
    "api": "pnpm --filter @shokupass/api",
    "ui": "pnpm --filter @shokupass/ui",
    "api-contracts": "pnpm --filter @shokupass/api-contracts"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "latest",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": "^18.18.2",
    "pnpm": "^8.11.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/*"
  ]
}
